# 大会運営ガイド

**応募締切から大会終了までに、何をどの順でやるかの手順書です。**
コマンドはコピーしてそのまま貼れます。表示される内容も実際のものを載せてあります。

サイトの直しかたは [詳細設計書](detail-design.md)、
画像の差し替えは [画像ガイド](image-guide.md) を見てください。

> **同じ内容の Excel 版が `docs/大会運営ガイド.xlsx` にあります。**
> 手順ごとにシートが分かれていて、最後のシートは印刷して使えるチェックリストです。
> **正本はこの Markdown のほう**なので、直すときはこちらを編集して
> `python tools/build_operation_xlsx.py` を流し直してください。

---

## 0. 先に済ませておくこと

大会当日になってから気づくと困るものです。**今のうちに確認してください。**

| やること | 状態 | どこで |
|---|---|---|
| Python が動くか確認 | — | `python --version` で 3.x が出ればOK |
| 応募フォームのURL | **設定済み** | `index.html` の `<!-- EDIT: ここをGoogleフォームのURLに` |
| GitHub Pages | **公開中** | https://fuku-hiro112.github.io/nenekko-cup/ |
| 開催日時 | 2026年8月30日（日）20:00 | 掲載済み |
| 応募締切 | 2026年8月23日（日）23:59 | 掲載済み |

> **告知の前に、ENTRY のボタンから実際にフォームが開くか一度押して確かめてください。**
> URL を差し替えたときも同じです。ここが死んでいると応募が1件も来ません。

動作確認は次の1行でできます。エラーが出なければ準備完了です。

```bash
python tools/draw_tables.py tools/participants.example.csv
```

---

## 1. 応募が締め切られたら（前日）

### 手順 1-1. 参加者のCSVを用意する

**Googleフォームの回答をスプレッドシートに出して、CSVで書き出すだけです。**
列を並べ替える必要はありません。

```csv
タイムスタンプ,お名前,ポカジャンを解放していますか
2026/08/10 21:03:11,ふろん,解放
2026/08/10 21:05:42,てるてる,未解放
```

見出しに「名前」「解放」という言葉が入っていれば、**その列を自動で見つけます。**
1列目がタイムスタンプでも問題ありません。

見出しが無い／言葉が違う場合は、1列目を名前・2列目を解放として読みます。
最低限これだけあれば動きます。

```csv
名前,解放
ふろん,解放
てるてる,未解放
```

解放の欄は、**空欄・「未」・「未解放」・「×」・「x」・「no」なら未解放**、
それ以外（「解放」「○」「o」「yes」など）は解放済みとして扱います。

### 手順 1-2. 抽選する

```bash
python tools/draw_tables.py 参加者.csv --save tournament.json
```

こう表示されます。

```
読んだ列: 名前 = 1列目「名前」 / 解放 = 2列目「解放」
参加者 8人（解放 4 / 未解放 4）
抽選 seed: 814   ← 同じ結果を出すには --seed 814

[卓1]
   Fukagami  ルーム作成
   アオ（未解放）
   てるてる（未解放）
   花園美咲
[卓2]
   アシエンSS  ルーム作成
   しゃるろって（未解放）
   ふろん
   フク（未解放）

進行（各卓から上位2名が勝ち上がり）
   1回戦: 8人 / 2卓
   決勝: 4人 / 1卓
```

**まず「読んだ列」を確認してください。** ここが違っていると、名前と解放を
取り違えたまま抽選されます。

- 各卓の**いちばん上の人が「ルーム作成」**です。解放している人が必ず1人入ります
- 4人に満たない卓には、そのまま開始すると**空席にCPUが入ります**
- `seed` を控えておけば、`--seed 814` で同じ卓分けを何度でも出せます

### 手順 1-3. 進行に無理がないか見る

「気になる点」が出たら読んでください。**出なければそのままで大丈夫です。**

```
気になる点
   - 決勝が3人しかいません（1席がCPU）。--advance を変えるか、参加人数の区切りを調整すると4人にできます。
```

各卓から何名が勝ち上がるかは `--advance` で変えられます（既定は2名）。

```bash
python tools/draw_tables.py 参加者.csv --advance 2 --save tournament.json
```

> **上位1名だけにすると人数が急に減ります。** 10人なら決勝が3人になり、
> 決勝までCPU入りになります。基本は既定の2名のままで問題ありません。

### 手順 1-4. Discordに貼る用がほしいとき

```bash
python tools/draw_tables.py 参加者.csv --seed 814 --md
```

そのままコピーして貼れる形で出ます。**卓分けを当日発表する方針なら、これは当日でOKです。**

---

## 2. 当日

### 手順 2-1. 卓分けをスプレッドシートに貼る

前日に出した卓分けを、スプレッドシートに貼り付けます。列は4つだけです。

| 回戦 | 卓 | 参加者 | 勝ち |
|---|---|---|---|
| 1回戦 | 卓1 | ふろん | |
| 1回戦 | 卓1 | てるてる | |

貼り付けた時点で、**サイトを開き直せばもう表示されます。**

> **初回だけ、シートの用意が必要です。** やりかたは章6を見てください。
> 一度そろえてしまえば、当日は「勝ち」に印を入れるだけになります。

### 手順 2-2. 対局が終わったら「勝ち」に印を入れる

**スプレッドシートの「勝ち」の欄に ○ を入れるだけです。**
パソコンでもスマホでも、Googleスプレッドシートが開ければ誰でもできます。

| 回戦 | 卓 | 参加者 | 勝ち |
|---|---|---|---|
| 1回戦 | 卓1 | ふろん | ○ |
| 1回戦 | 卓1 | てるてる | |
| 1回戦 | 卓1 | アシエンSS | ○ |
| 1回戦 | 卓1 | どこばか | |

- **○でなくても構いません。** 何か書いてあれば勝ちとして扱います（`o` でも `1` でも可）
- **まだ終わっていない卓は空のまま**にしてください
- **2回戦以降の行は書かなくて大丈夫です。** 全部の卓に印が入ると、
  勝った人を集めて次の回戦が自動で組まれます

### 手順 2-3. サイトを開き直す

**それだけです。** ページを開くたびにスプレッドシートを読みに行くので、
`git` も `python` も要りません。反映まで**数十秒**かかることがあります。

**2回戦以降も 2-2 の繰り返しです。**

### 手順 2-4. 決勝

決勝の行は自動で作られます。**優勝者1名にだけ○を入れてください。**
そこだけ濃い金色で「優勝」と表示されます。

> 見出しが「決勝」の回戦だけ「優勝」が出ます。参加者が4人以下で
> 1回戦がそのまま決勝になる場合は、シートの「回戦」欄に**「決勝」と書いてください**。

### （任意）最終結果をページに焼き付ける

大会が終わったあと、スプレッドシートを消しても表が残るようにしたいときだけ、
主催者のパソコンで次を実行します。**当日は不要です。**

```bash
python tools/update_bracket.py tournament.json
git add -A && git commit -m "最終結果" && git push
```

---

## 3. 飛び入りが来たとき

**対局が始まる前なら、参加者CSVに行を足して手順1-2からやり直してください。**
卓数も段数も人数に合わせて組み直されます。

```bash
python tools/draw_tables.py 参加者.csv --save tournament.json   # seedは付けない＝引き直し
python tools/update_bracket.py tournament.json
```

> **1回戦が始まったあとに来た人をどうするかは決めていません。**
> 「開始時刻で締め切り、それ以降は観戦のみ」と先に決めておくと揉めません。

---

## 4. うまくいかないとき

### 「同じ名前が複数あります」

```
同じ名前が複数あります: A
当日の呼び分けができないので、区別できる名前にしてください。
```

CSVに同じ表示名が2人います。当日どちらを呼んでいるか分からなくなるため止めています。
片方に印を付けるなどして区別してください。

### 「解放している人が足りません」

```
解放している人が足りません。2卓に対して解放者は1人です。
各卓にルームを作れる人が1人ずつ要るので、卓を減らす（＝1卓あたりの人数を増やす）か、解放者を増やしてください。
```

**各卓にルームを作れる人が1人ずつ必要**なので、解放者が卓数より少ないと組めません。
当日その場で解放してもらうか、卓数を減らす（人数の多い卓を作る）判断が要ります。

### 「〜に『◯◯』がいません」

```
1回戦 卓1 に「ふくがみ」がいません。その卓の参加者は Fukagami、アオ、てるてる、花園美咲 です。名前の表記を合わせてください。
```

`winners` に書いた名前が、その卓の `players` と一致していません。
**表示されている名前をそのままコピーしてください。** 大文字小文字や表記ゆれも区別します。

### 表が「当日をお楽しみに」のまま変わらない

スプレッドシートが読めていません。上から順に確かめてください。

1. **`index.html` の `data-sheet` にIDが入っているか**（章6-3）。空だと何も読みに行きません
2. **共有設定が「リンクを知っている全員」になっているか**（章6-2）。
   自分だけが見られる状態だと、サイトからは読めません
3. **シート名が合っているか。** タブの名前を変えたなら `data-sheet-name` にも同じ名前を入れます
4. **見出しの行があるか。** 1行目は `回戦 / 卓 / 参加者 / 勝ち` にしてください
5. 少し待ってから開き直す。反映まで数十秒かかることがあります

読めなくても**ページの他の部分は普通に動きます**。表だけが出ない状態です。

### 次の回戦が出てこない

その回戦の**どれか1卓でも「勝ち」が空**だと、次には進みません。
全部の卓に印が入っているか確かめてください。**わざとそうしてあります**（対局中に
先の組み合わせが出てしまわないようにするため）。

### 「決勝」なのに「優勝」と出ない

シートの「回戦」欄が**「決勝」になっているか**確かめてください。
自動で作られた最後の回戦は「決勝」になりますが、手で書いた場合は
「1回戦」のままだと優勝表示になりません。

### 「BRACKET:START / BRACKET:END が見つかりません」

`index.html` からマーカーのコメントを消してしまっています。
消してしまった場合は BRACKET セクションの中に、次の2行を戻してください。

```html
      <!-- BRACKET:START -->
      <!-- BRACKET:END -->
```

### 書き換わってほしくないのに上書きされる

`<!-- BRACKET:START -->` 〜 `<!-- BRACKET:END -->` の**間は毎回作り直されます。**
手で書いた内容は次の実行で消えます。前後の文章（見出しやリード文）は消えません。

### とりあえず結果だけ見たい

```bash
python tools/update_bracket.py tournament.json --dry
```

`index.html` を書き換えずに、出力される内容だけ確認できます。

---

## 5. 主催者が動けないときのために

**当日の更新は、パソコンの操作ができない人でもできます。**
スプレッドシートの「勝ち」欄に印を入れるだけだからです。

代わりの人にお願いするときは、次の2つを渡してください。

1. **スプレッドシートの編集リンク**（Googleアカウントがあれば誰でも編集できます）
2. この章と章2の手順

必要ないもの：GitHubアカウント、Python、git、パソコン。**スマホだけで足ります。**

> **サイトを開き直すのは参加者側の操作です。** 代わりの人が印を入れたあと、
> 誰かがページを更新すれば最新の表が出ます。特別な作業は要りません。

---

## 6. スプレッドシートの用意（最初の1回だけ）

### 手順 6-1. シートを作る

Googleスプレッドシートを新規作成し、1行目に見出しを入れます。

| 回戦 | 卓 | 参加者 | 勝ち |
|---|---|---|---|

`tools/bracket-sheet.example.csv` をインポートすると、この形が入った状態から始められます。

### 手順 6-2. 「リンクを知っている全員」に共有する

右上の「共有」→ 一般的なアクセス →「リンクを知っている全員」→ **閲覧者**。

> **編集者ではなく閲覧者にしてください。** サイトは読むだけなので閲覧権限で足ります。
> 更新を任せる人には、別途「編集者」として個別に追加してください。

### 手順 6-3. IDをページに書く

スプレッドシートのURLはこの形です。

```
https://docs.google.com/spreadsheets/d/【この部分がID】/edit#gid=0
```

`index.html` の `<!-- EDIT: トーナメント表のスプレッドシートID -->` を探し、
`data-sheet="..."` にそのIDを貼ります。シート名を変えている場合は
`data-sheet-name="シート1"` も埋めてください（既定のままなら空でOK）。

```html
<div class="bracket" data-sheet="1AbCdEf...XyZ" data-sheet-name="">
```

そのあと commit して push すれば準備完了です。

> **IDが空のあいだは「当日をお楽しみに」が出たままになります。** 壊れはしません。

### 仕組みについて

サイトは Google の公開データ機能（gviz）を使ってシートを読んでいます。
**Googleのスプレッドシートは `Access-Control-Allow-Origin` を返さないため、
ふつうの読み込み方（fetch）では取得できません。** 実測して確認したうえで、
制限を受けない JSONP という方式にしてあります。

読めなかったとき（未設定・非公開・通信不良・JSが動かない）は、
**「当日をお楽しみに」の表示に戻るだけ**で、ページの他の部分は普通に動きます。

---

## 7. 当日の持ち物チェック

- [ ] スプレッドシートに**1回戦の卓分けが入っている**
- [ ] サイトを開いて、その卓分けが**表として出ることを確認した**
- [ ] スプレッドシートが「リンクを知っている全員／閲覧者」になっている
- [ ] **代わりの人に編集リンクを渡してある**（章5）
- [ ] Discord のイベントルームと、卓の数だけVCがある（`卓1` `卓2` …）
- [ ] 主催者のDMが開いている（フッターの問い合わせ先になっています）

> **当日にパソコンは要りません。** 更新はスプレッドシートの「勝ち」に印を入れるだけで、
> `git` も `python` も使いません。前日の抽選（章1）だけパソコンで行います。
